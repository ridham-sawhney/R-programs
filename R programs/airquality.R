getwd()
data()
airquality 
str(airquality )
head(airquality )
tail(airquality )
attach(airquality )   #store diif subdata in diff variables i.e temp is used as variable wgich is storing temperature data
hist(Temp)
